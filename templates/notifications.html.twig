{% extends 'base.html.twig' %}

{% block body %}

    {% if notifs|length > 0 %}
        {% for n in notifs %}
            {{ include('_notif_element_liste.html.twig', {notif: n}) }}
        {% endfor %}
    {% endif %}

{% endblock %}